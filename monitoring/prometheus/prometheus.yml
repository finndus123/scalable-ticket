global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'ticket-api'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # Assuming ticket-api runs on host port 8080
      - targets: ['host.docker.internal:8080']
